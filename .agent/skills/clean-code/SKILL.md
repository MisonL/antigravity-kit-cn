---
name: clean-code
description: 务实的编码标准—— 简洁、直接、不进行过度设计、不编写无用注释。
allowed-tools: Read, Write, Edit
version: 2.0
priority: CRITICAL
---

# 整洁代码 - 务实的 AI 编码标准

> **核心技能** —— 保持**简洁、直接并专注于解决方案**。

---

## 核心原则

| 原则 | 规则 |
| ---- | ---- |
| **SRP** | 单一职责原则 —— 每个函数/类只做**一件**事情 |
| **DRY** | 不要重复自己 —— 提取重复项，重复利用 |
| **KISS** | 保持简单 —— 采用能跑通的最简单方案 |
| **YAGNI** | 你不会需要它 —— 不要构建未被要求的功能 |
| **童子军军规** | 离开时让代码比你来时更整洁 |

---

## 命名规则

| 元素 | 规范 |
| ---- | ---- |
| **变量** | 揭示意图：用 `userCount` 而非 `n` |
| **函数** | 动词 + 名词：用 `getUserById()` 而非 `user()` |
| **布尔值** | 提问形式：`isActive`, `hasPermission`, `canEdit` |
| **常量** | 全大写蛇形命名（SCREAMING_SNAKE）：`MAX_RETRY_COUNT` |

> **准则：** 如果你需要通过注释来解释一个名字，请直接重命名。

---

## 函数规则

| 规则 | 描述 |
| ---- | ---- |
| **短小** | 最多 20 行，理想情况下 5-10 行 |
| **专注** | 只做一件事，并把它做好 |
| **层次** | 每个函数只包含一个抽象层级 |
| **参数少** | 最多 3 个参数，优先选择 0-2 个 |
| **无副作用** | 不要产生预期之外的输入状态改变（mutation） |

---

## 代码结构

| 模式 | 应用建议 |
| ---- | -------- |
| **卫语句（Guard Clauses）** | 针对边缘情况及早返回（early return） |
| **扁平化 > 嵌套** | 避免深度嵌套（最多 2 层） |
| **组合（Composition）** | 将短小的函数组合在一起使用 |
| **就近原则（colocation）** | 保持相关代码的物理距离尽可能接近 |

---

## AI 编码风格

| 场景 | 行动建议 |
| ---- | -------- |
| 老板要求一个功能 | 直接编写代码实现它 |
| 老板报告一个 Bug | 修复它，不要多做解释 |
| 需求不明确 | 直接询问，不要自行假设 |

---

## 反模式（应避免的行为）

| ❌ 错误模式 | ✅ 推荐修复方案 |
| ---------- | -------------- |
| 每一行都写注释 | 删除显而易见的注释 |
| 为单行逻辑封装 Helper（辅助函数） | 直接内联（inline）代码 |
| 为 2 个对象写工厂模式 | 直接实例化 |
| 只有 1 个函数的 utils.ts | 代码放在其被使用的地方 |
| “首先我们导入……” | 直接编写核心代码 |
| 深度嵌套逻辑 | 使用卫语句（Guard Clauses） |
| 使用魔术数字 | 使用具名常量 |
| 万能函数（god functions） | 按职责进行拆分 |

---

## 🔴 在编辑任何文件之前（先思考！）

**在修改文件前，请先扪心自问：**

| 提问 | 为什么要问 |
| ---- | ---------- |
| **谁引用了这个文件？** | 修改可能会破坏它们 |
| **这个文件引用了谁？** | 接口（Interface）可能需要变更 |
| **有哪些测试覆盖了这里？** | 测试可能会失败 |
| **这是共享组件吗？** | 可能影响多个地方 |

**快速检查示例：**

```
待编辑文件：UserService.ts
└── 谁引用了它？ → UserController.ts, AuthController.ts
└── 它们也需要改吗？ → 检查函数签名是否一致
```

> 🔴 **准则：** 在**同一个**任务中同时编辑该文件及其所有受影响的依赖文件。
> 🔴 **绝不允许留下运行失败的引用或缺失的更新。**

---

## 总结

| 推荐做法（Do） | 禁止行为（Don't） |
| ------------- | ---------------- |
| 直接编写代码 | 编写教程式的引导 |
| 让代码自文档化 | 添加显而易见的注释 |
| 立即修复 Bug（缺陷） | 先解释修复方案 |
| 内联短小代码 | 创建不必要的琐碎文件 |
| 命名清晰准确 | 使用缩写 |
| 保持函数短小 | 编写超过 100 行的函数 |

> **谨记：老板想要的是能运行的代码，而不是一堂编程课。**

---

## 🔴 完成前的自我检查（强制执行）

**在说“任务完成”之前，请验证：**

| 检查项 | 确认问题 |
| ------ | -------- |
| ✅ **目标达成了吗？** | 我是否精准完成了老板的要求？ |
| ✅ **文件都改了吗？** | 我是否修改了所有必要的相关文件？ |
| ✅ **代码能跑吗？** | 我是否测试/验证了该变更？ |
| ✅ **没有报错吗？** | Lint（规范检查）和 TypeScript（类型系统）检查是否通过？ |
| ✅ **没遗漏什么吗？** | 是否遗漏了任何边缘情况？ |

> 🔴 **准则：** 如果以上任何一项检查失败，请在宣布完成前先修复它。

---

## 验证脚本（强制执行）

> 🔴 **核心要点：** 每个代理智能体在完成工作后，**仅**运行其所属技能的特定验证脚本。

### 代理 → 脚本映射关系

| 代理 | 验证脚本 | 执行命令 |
| ------------ | -------- | -------- |
| **frontend-specialist** | UX 审计 | `python .agent/skills/frontend-design/scripts/ux_audit.py .` |
| **frontend-specialist** | 无障碍检查 | `python .agent/skills/frontend-design/scripts/accessibility_checker.py .` |
| **backend-specialist** | API 校验 | `python .agent/skills/api-patterns/scripts/api_validator.py .` |
| **mobile-developer** | 移动端审计 | `python .agent/skills/mobile-design/scripts/mobile_audit.py .` |
| **database-architect** | 模式校验 | `python .agent/skills/database-design/scripts/schema_validator.py .` |
| **security-auditor** | 安全扫描 | `python .agent/skills/vulnerability-scanner/scripts/security_scan.py .` |
| **seo-specialist** | SEO 检查 | `python .agent/skills/seo-fundamentals/scripts/seo_checker.py .` |
| **seo-specialist** | GEO 检查 | `python .agent/skills/geo-fundamentals/scripts/geo_checker.py .` |
| **performance-optimizer** | Lighthouse 审计 | `python .agent/skills/performance-profiling/scripts/lighthouse_audit.py <网址>` |
| **test-engineer** | 测试运行 | `python .agent/skills/testing-patterns/scripts/test_runner.py .` |
| **test-engineer** | Playwright E2E | `python .agent/skills/webapp-testing/scripts/playwright_runner.py <网址>` |
| **任何代理** | Lint 检查 | `python .agent/skills/lint-and-validate/scripts/lint_runner.py .` |
| **任何代理** | 类型覆盖率 | `python .agent/skills/lint-and-validate/scripts/type_coverage.py .` |
| **任何代理** | i18n 检查 | `python .agent/skills/i18n-localization/scripts/i18n_checker.py .` |

> ❌ **错误做法：** `test-engineer` 去运行 `ux_audit.py`
> ✅ **正确做法：** `frontend-specialist` 去运行 `ux_audit.py`

---

### 🔴 脚本输出处理规则（阅读 → 总结 → 询问）

**运行验证脚本时，你必须：**

1. **执行脚本**并捕获全量输出。
2. **解析输出** —— 识别出错误、警告以及通过项。
3. **按以下格式向老板汇总：**

```markdown
## 脚本运行结果：[脚本名称.py]

### ❌ 发现错误（X 项）

- [文件:行号] 错误描述 1
- [文件:行号] 错误描述 2

### ⚠️ 警告事项（Y 项）

- [文件:行号] 警告描述

### ✅ 已通过（Z 项）

- 检查项 1 已通过
- 检查项 2 已通过

**我应该修复这 X 个错误吗？**
```

4. **等待老板确认**后再进行修复。
5. **修复后** —— 重新运行脚本以确认修复成功。

> 🔴 **违规逻辑：** 运行脚本后忽略输出结果 = 任务失败。
> 🔴 **违规逻辑：** 不经询问即自动进行修复 = 不被允许。
> 🔴 **准则：** 始终遵循 阅读输出 → 总结 → 询问 → 修复 的闭环流程。

---
