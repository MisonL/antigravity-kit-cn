---
name: mobile-design
description: Mobile-first design thinking and decision-making for iOS and Android apps. Touch interaction, performance patterns, platform conventions. Teaches principles, not fixed values. Use when building React Native, Flutter, or native mobile apps.
allowed-tools: Read, Glob, Grep, Bash
---

# 移动端设计系统

> **哲学:** 触控优先。关注电池。尊重平台。具备离线能力。
> **核心原则:** 移动端不是小型桌面端。思考移动端约束，询问平台选择。

---

## 🔧 运行时脚本

**执行这些脚本进行验证 (不要阅读，直接运行)：**

| 脚本                      | 用途                 | 用法                                            |
| ------------------------- | -------------------- | ----------------------------------------------- |
| `scripts/mobile_audit.py` | 移动端 UX 与触控审计 | `python scripts/mobile_audit.py <project_path>` |

---

## 🔴 强制：工作前先阅读参考文件！

**⛔ 在阅读相关文件之前，不要开始开发：**

### 通用 (始终阅读)

| 文件                                                       | 内容                                                                                | 状态                  |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------- |
| **[mobile-design-thinking.md](mobile-design-thinking.md)** | **⚠️ 反背诵：强制思考，防止 AI 默认行为**                                           | **⬜ CRITICAL FIRST** |
| **[touch-psychology.md](touch-psychology.md)**             | **菲茨定律 (Fitts' Law), 手势 (gestures), 触觉反馈 (haptics), 拇指区 (thumb zone)** | **⬜ CRITICAL**       |
| **[mobile-performance.md](mobile-performance.md)**         | **RN/Flutter 性能, 60fps, 内存**                                                    | **⬜ CRITICAL**       |
| **[mobile-backend.md](mobile-backend.md)**                 | **推送通知, 离线同步, 移动端 API**                                                  | **⬜ CRITICAL**       |
| **[mobile-testing.md](mobile-testing.md)**                 | **测试金字塔, E2E, 平台特定**                                                       | **⬜ CRITICAL**       |
| **[mobile-debugging.md](mobile-debugging.md)**             | **Native vs JS 调试, Flipper, Logcat**                                              | **⬜ CRITICAL**       |
| [mobile-navigation.md](mobile-navigation.md)               | Tab/Stack/Drawer, 深度链接 (deep linking)                                           | ⬜ Read               |
| [mobile-typography.md](mobile-typography.md)               | 系统字体 (System fonts), 动态字体 (Dynamic Type), 无障碍 (a11y)                     | ⬜ Read               |
| [mobile-color-system.md](mobile-color-system.md)           | OLED, 暗色模式 (dark mode), 电池感知 (battery-aware)                                | ⬜ Read               |
| [decision-trees.md](decision-trees.md)                     | 框架/状态/存储选择 (Framework/state/storage selection)                              | ⬜ Read               |

> 🧠 **mobile-design-thinking.md 是优先级！** 此文件确保 AI 进行思考，而不是使用死记硬背的模式。

### 平台特定 (根据目标阅读)

| 平台               | 文件                                       | 内容                                             | 何时阅读               |
| ------------------ | ------------------------------------------ | ------------------------------------------------ | ---------------------- |
| **iOS**            | [platform-ios.md](platform-ios.md)         | Human Interface Guidelines, SF Pro, SwiftUI 模式 | 为 iPhone/iPad 构建时  |
| **Android**        | [platform-android.md](platform-android.md) | Material Design 3, Roboto, Compose 模式          | 为 Android 构建时      |
| **Cross-Platform** | 以上两者                                   | 平台差异点                                       | React Native / Flutter |

> 🔴 **如果为 iOS 构建 → 先读 platform-ios.md！**
> 🔴 **如果为 Android 构建 → 先读 platform-android.md！**
> 🔴 **如果跨平台 → 阅读两者并应用条件平台逻辑！**

---

## ⚠️ 关键：假设前先询问 (强制)

> **STOP! 如果用户的请求是开放式的，不要默认为你的喜好。**

### 如果未指定，你必须询问:

| 方面                          | 询问                                            | 为什么             |
| ----------------------------- | ----------------------------------------------- | ------------------ |
| **平台 (Platform)**           | "iOS, Android, 还是两者？"                      | 影响每一个设计决策 |
| **框架 (Framework)**          | "React Native, Flutter, 还是原生？"             | 决定模式和工具     |
| **导航 (Navigation)**         | "标签栏, 抽屉, 还是基于堆栈？"                  | 核心 UX 决策       |
| **状态 (State)**              | "什么状态管理？ (Zustand/Redux/Riverpod/BLoC?)" | 架构基础           |
| **离线 (Offline)**            | "这需要离线工作吗？"                            | 影响数据策略       |
| **目标设备 (Target devices)** | "仅手机，还是支持平板？"                        | 布局复杂性         |

### ⛔ AI 移动端反模式 (禁止列表)

> 🚫 **这是一些必须避免的 AI 默认倾向！**

#### 性能之罪

| ❌ 绝不 (NEVER DO)           | 为什么错                             | ✅ 总是 (ALWAYS DO)                                |
| ---------------------------- | ------------------------------------ | -------------------------------------------------- |
| **长列表用 ScrollView**      | 渲染所有项，内存爆炸                 | 使用 `FlatList` / `FlashList` / `ListView.builder` |
| **内联 renderItem 函数**     | 每次渲染都创建新函数，所有项重新渲染 | `useCallback` + `React.memo`                       |
| **缺失 keyExtractor**        | 基于索引的键导致重新排序时的 Bug     | 来自数据的唯一、稳定 ID                            |
| **跳过 getItemLayout**       | 异步布局 = 滚动卡顿                  | 当项有固定高度时提供                               |
| **到处都是 setState()**      | 不必要的 Widget 重建                 | 针对性状态, `const` 构造函数                       |
| **Native driver: false**     | 动画被 JS 线程阻塞                   | 总是 `useNativeDriver: true`                       |
| **生产环境中的 console.log** | 严重阻塞 JS 线程                     | 发布构建前移除                                     |
| **跳过 React.memo/const**    | 任何更改都会导致所有项重新渲染       | 总是记忆列表项                                     |

#### 触控/UX 之罪

| ❌ 绝不 (NEVER DO)  | 为什么错               | ✅ 总是 (ALWAYS DO)                    |
| ------------------- | ---------------------- | -------------------------------------- |
| **触控目标 < 44px** | 无法准确点击，令人沮丧 | 最小 44pt (iOS) / 48dp (Android)       |
| **目标间距 < 8px**  | 误触邻居               | 最小 8-12px 间隙                       |
| **仅手势交互**      | 运动障碍用户被排除     | 总是提供按钮替代方案                   |
| **无加载状态**      | 用户认为应用崩溃       | 总是显示加载反馈                       |
| **无错误状态**      | 用户卡住，无恢复路径   | 显示带有重试选项的错误                 |
| **无离线处理**      | 网络丢失时崩溃/阻塞    | 优雅降级，缓存数据                     |
| **忽略平台惯例**    | 用户困惑，肌肉记忆破坏 | iOS 感觉像 iOS, Android 感觉像 Android |

#### 安全之罪

| ❌ 绝不 (NEVER DO)        | 为什么错                     | ✅ 总是 (ALWAYS DO)                                       |
| ------------------------- | ---------------------------- | --------------------------------------------------------- |
| **AsyncStorage 中的令牌** | 易于访问，在 Root 设备上被盗 | `SecureStore` / `Keychain` / `EncryptedSharedPreferences` |
| **硬编码 API 密钥**       | 从 APK/IPA 逆向工程          | 环境变量，安全存储                                        |
| **跳过 SSL 钉扎**         | 可能发生中间人攻击           | 生产环境中钉扎证书                                        |
| **记录敏感数据**          | 日志可被提取                 | 从不记录令牌、密码、PII                                   |

#### 架构之罪

| ❌ 绝不 (NEVER DO)     | 为什么错                 | ✅ 总是 (ALWAYS DO)      |
| ---------------------- | ------------------------ | ------------------------ |
| **UI 中的业务逻辑**    | 不可测试，不可维护       | 服务层分离               |
| **一切皆全局状态**     | 不必要的重新渲染，复杂性 | 默认局部状态，需要时提升 |
| **事后才考虑深度链接** | 通知，分享损坏           | 从第一天开始规划深度链接 |
| **跳过处置/清理**      | 内存泄漏，僵尸监听器     | 清理订阅，定时器         |

---

## 📱 平台决策矩阵

### 何时统一与分歧

```
                    统一（两端相同）                         分歧（平台特定）
                    ─────────────────                       ───────────────────────
业务逻辑 ✅ 始终统一                                          -
数据层 ✅ 始终统一                                            -
核心功能 ✅ 始终统一                                          -

导航         -                                              ✅ iOS: 边缘滑动，Android: 返回按钮
手势         -                                              ✅ 符合平台原生交互习惯
图标         -                                              ✅ SF Symbols 与 Material Icons 区分使用
日期选择器   -                                              ✅ 使用各平台原生选择器体验
模态/表单    -                                              ✅ iOS 常用 bottom sheet，Android 常用 dialog
排版         -                                              ✅ SF Pro 与 Roboto 可按平台分流
错误对话框   -                                              ✅ 遵循平台警报与确认交互惯例
```

### 快速参考：平台默认值

| 元素                                | iOS                          | Android                                        |
| ----------------------------------- | ---------------------------- | ---------------------------------------------- |
| **主要字体 (Primary Font)**         | SF Pro / SF Compact          | Roboto                                         |
| **最小触控目标 (Min Touch Target)** | 44pt × 44pt                  | 48dp × 48dp                                    |
| **返回导航 (Back Navigation)**      | 左边缘滑动 (Edge swipe left) | 系统返回按钮/手势 (System back button/gesture) |
| **底部标签图标 (Bottom Tab Icons)** | SF Symbols                   | Material Symbols                               |
| **动作表 (Action Sheet)**           | 从底部 UIActionSheet         | Bottom Sheet / Dialog                          |
| **进度 (Progress)**                 | 旋转器 (Spinner)             | 线性进度 (Linear progress) (Material)          |
| **下拉刷新 (Pull to Refresh)**      | Native UIRefreshControl      | SwipeRefreshLayout                             |

---

## 🧠 移动端 UX 心理学 (快速参考)

### 触控的菲茨定律

```
桌面端: 光标精确 (1px)
移动端: 手指不精确 (~7mm 接触面积)

→ 触控目标最小 44-48px
→ 重要动作放在拇指区（屏幕底部）
→ 破坏性动作远离易触达区域
```

### 拇指区 (单手使用)

```
┌─────────────────────────────┐
│      HARD TO REACH          │ ← 难触达：导航，菜单，返回
│        (伸展)               │
├─────────────────────────────┤
│      OK TO REACH            │ ← 可触达：次要动作
│       (自然)                │
├─────────────────────────────┤
│      EASY TO REACH          │ ← 易触达：主要 CTA，标签栏
│    (拇指自然弧度)           │ ← 主内容交互
└─────────────────────────────┘
        [  HOME  ]
```

### 移动端特定认知负荷

| 桌面端     | 移动端差异         |
| ---------- | ------------------ |
| 多窗口     | 一次一个任务       |
| 键盘快捷键 | 触控手势           |
| 悬停状态   | 无悬停 (点击或无)  |
| 大视口     | 有限空间，垂直滚动 |
| 稳定注意力 | 不断被打断         |

有关深潜：[touch-psychology.md](touch-psychology.md)

---

## ⚡ 性能原则 (快速参考)

### React Native 关键规则

```typescript
// ✅ 正确：记忆化 renderItem + React.memo 包装器
const ListItem = React.memo(({ item }: { item: Item }) => (
  <View style={styles.item}>
    <Text>{item.title}</Text>
  </View>
));

const renderItem = useCallback(
  ({ item }: { item: Item }) => <ListItem item={item} />,
  []
);

// ✅ 正确：具有所有优化的 FlatList
<FlatList
  data={items}
  renderItem={renderItem}
  keyExtractor={(item) => item.id}  // 稳定 ID，不是索引
  getItemLayout={(data, index) => ({
    length: ITEM_HEIGHT,
    offset: ITEM_HEIGHT * index,
    index,
  })}
  removeClippedSubviews={true}
  maxToRenderPerBatch={10}
  windowSize={5}
/>
```

### Flutter 关键规则

```dart
// ✅ 正确：const 构造函数防止重建
class MyWidget extends StatelessWidget {
  const MyWidget({super.key}); // CONST!

  @override
  Widget build(BuildContext context) {
    return const Column( // CONST!
      children: [
        Text('Static content'),
        MyConstantWidget(),
      ],
    );
  }
}

// ✅ 正确：使用 ValueListenableBuilder 的针对性状态
ValueListenableBuilder<int>(
  valueListenable: counter,
  builder: (context, value, child) => Text('$value'),
  child: const ExpensiveWidget(), // 不会重建！
)
```

### 动画性能

```
GPU 加速 (快):               CPU 绑定 (慢):
├── transform               ├── width, height
├── opacity                 ├── top, left, right, bottom
└── (仅使用这些)             ├── margin, padding
                            └── (避免动画化这些)
```

有关完整指南：[mobile-performance.md](mobile-performance.md)

---

## 📝 检查点 (任何移动端工作前强制)

> **在编写任何移动端代码之前，你必须完成此检查点：**

```
🧠 检查点：

平台：   [ iOS / Android / 两者 ]
框架：   [ React Native / Flutter / SwiftUI / Kotlin ]
已读文件： [ 列出你已阅读的技能文件 ]

我将应用的 3 个原则：
1. _______________
2. _______________
3. _______________

我将避免的反模式：
1. _______________
2. _______________
```

**示例:**

```
🧠 检查点：

平台:   iOS + Android (跨平台)
框架:   React Native + Expo
已读文件: touch-psychology.md, mobile-performance.md, platform-ios.md, platform-android.md

我将应用的 3 个原则:
1. 所有列表使用 FlatList + React.memo + useCallback
2. 48px 触控目标，主要 CTA 在拇指区
3. 平台特定导航 (iOS 边缘滑动，Android 返回按钮)

我将避免的反模式:
1. 列表使用 ScrollView → FlatList
2. 内联 renderItem → Memoized
3. 令牌存储在 AsyncStorage → SecureStore
```

> 🔴 **无法填写检查点？ → 返回并阅读技能文件。**

---

## 🔧 框架决策树

```
你在构建什么？
        │
        ├── 需要 OTA 更新 + 快速迭代 + Web 团队
        │   └── ✅ React Native + Expo
        │
        ├── 需要像素级完美自定义 UI + 性能关键
        │   └── ✅ Flutter
        │
        ├── 深度原生功能 + 单一平台聚焦
        │   ├── iOS 专用 → SwiftUI
        │   └── Android 专用 → Kotlin + Jetpack Compose
        │
        ├── 现有 RN 代码库 + 新功能
        │   └── ✅ React Native (bare workflow)
        │
        └── 企业级 + 现有 Flutter 代码库
            └── ✅ Flutter
```

有关完整决策树：[decision-trees.md](decision-trees.md)

---

## 📋 开发前检查清单

### 开始任何移动端项目之前

- [ ] **平台已确认？** (iOS / Android / 两者)
- [ ] **框架已选择？** (RN / Flutter / Native)
- [ ] **导航模式已决定？** (标签栏 / 抽屉 / 基于堆栈)
- [ ] **状态管理已选择？** (Zustand / Redux / Riverpod / BLoC)
- [ ] **离线需求已知？**
- [ ] **深度链接从第一天开始规划？**
- [ ] **目标设备已定义？** (仅手机 / 平板 / 两者)

### 每个屏幕之前

- [ ] **触控目标 ≥ 44-48px？**
- [ ] **主要 CTA 在拇指区？**
- [ ] **存在加载状态？**
- [ ] **存在带有重试的错误状态？**
- [ ] **已考虑离线处理？**
- [ ] **遵循平台惯例？**

### 发布之前

- [ ] **console.log 已移除？**
- [ ] **敏感数据使用 SecureStore？**
- [ ] **SSL 钉扎已启用？**
- [ ] **列表已优化 (memo, keyExtractor)？**
- [ ] **卸载时清理内存？**
- [ ] **已在低端设备上测试？**
- [ ] **所有交互元素上的无障碍标签？**

---

## 📚 参考文件

有关特定领域的更深入指导：

| 文件                                                   | 何时使用                                              |
| ------------------------------------------------------ | ----------------------------------------------------- |
| [mobile-design-thinking.md](mobile-design-thinking.md) | **第一与关键! (FIRST!) 反背诵，强制基于上下文的思考** |
| [touch-psychology.md](touch-psychology.md)             | 理解触控交互，菲茨定律，手势设计                      |
| [mobile-performance.md](mobile-performance.md)         | 优化 RN/Flutter，60fps，内存/电池                     |
| [platform-ios.md](platform-ios.md)                     | iOS 特定设计，HIG 合规                                |
| [platform-android.md](platform-android.md)             | Android 特定设计，Material Design 3                   |
| [mobile-navigation.md](mobile-navigation.md)           | 导航模式，深度链接                                    |
| [mobile-typography.md](mobile-typography.md)           | 字体比例，系统字体，无障碍                            |
| [mobile-color-system.md](mobile-color-system.md)       | OLED 优化，暗色模式，电池                             |
| [decision-trees.md](decision-trees.md)                 | 框架，状态，存储决策                                  |

---

> **记住：** 移动用户是不耐烦的、被干扰的，并且在小屏幕上使用不精确的手指。**为最坏的情况设计 (Design for the WORST conditions):** 网络差 (bad network), 单手 (one hand), 强烈的阳光 (bright sun), 低电量 (low battery). 如果它在那里行得通，它在任何地方都行得通。
