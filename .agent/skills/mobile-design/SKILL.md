---
name: mobile-design
description: 移动端优先设计思维、触摸交互与平台规范 (iOS/Android)
allowed-tools: Read, Glob, Grep, Bash
---

# Mobile Design System - 移动端设计系统

> **哲学：** 触控优先。电池意识。尊重平台。离线能力。
> **核心原则：** 移动设备不是仅是一个小的桌面端。思考移动约束，询问平台选择。

---

## 🔧 运行时脚本 (Runtime Scripts)

**执行这些进行验证（不要读，直接运行）：**

| 脚本                      | 用途               | 用法                                            |
| ------------------------- | ------------------ | ----------------------------------------------- |
| `scripts/mobile_audit.py` | 移动 UX 与触控审计 | `python scripts/mobile_audit.py <project_path>` |

---

## 🔴 强制：工作前阅读参考文件！

**⛔ 在阅读相关文件之前，不要开始开发：**

### 通用 (始终阅读)

| 文件 (File)                                                | 内容 (Content)                                | 状态 (Status)                    |
| ---------------------------------------------------------- | --------------------------------------------- | -------------------------------- |
| **[mobile-design-thinking.md](mobile-design-thinking.md)** | **⚠️ 反死记硬背：强制思考，防止 AI 默认行为** | **⬜ 关键首选 (CRITICAL FIRST)** |
| **[touch-psychology.md](touch-psychology.md)**             | **费茨定律，手势，触觉，拇指区**              | **⬜ 关键 (CRITICAL)**           |
| **[mobile-performance.md](mobile-performance.md)**         | **RN/Flutter 性能，60fps，内存**              | **⬜ 关键 (CRITICAL)**           |
| **[mobile-backend.md](mobile-backend.md)**                 | **推送通知，离线同步，移动 API**              | **⬜ 关键 (CRITICAL)**           |
| **[mobile-testing.md](mobile-testing.md)**                 | **测试金字塔，E2E，平台特定**                 | **⬜ 关键 (CRITICAL)**           |
| **[mobile-debugging.md](mobile-debugging.md)**             | **原生 vs JS 调试，Flipper，Logcat**          | **⬜ 关键 (CRITICAL)**           |
| [mobile-navigation.md](mobile-navigation.md)               | Tab/Stack/Drawer，深度链接                    | ⬜ 阅读                          |
| [mobile-typography.md](mobile-typography.md)               | 系统字体，动态类型，a11y                      | ⬜ 阅读                          |
| [mobile-color-system.md](mobile-color-system.md)           | OLED，暗色模式，电池感知                      | ⬜ 阅读                          |
| [decision-trees.md](decision-trees.md)                     | 框架/状态/存储选择                            | ⬜ 阅读                          |

> 🧠 **mobile-design-thinking.md 是重中之重！** 此文件确保 AI 思考而不是使用死记硬背的模式。

### 平台特定 (根据目标阅读)

| 平台               | 文件                                       | 内容                                    | 何时阅读               |
| ------------------ | ------------------------------------------ | --------------------------------------- | ---------------------- |
| **iOS**            | [platform-ios.md](platform-ios.md)         | 人机界面指南，SF Pro，SwiftUI 模式      | 为 iPhone/iPad 构建    |
| **Android**        | [platform-android.md](platform-android.md) | Material Design 3，Roboto，Compose 模式 | 为 Android 构建        |
| **Cross-Platform** | 两者都读                                   | 平台分歧点                              | React Native / Flutter |

> 🔴 **如果为 iOS 构建 → 首先阅读 platform-ios.md！**
> 🔴 **如果为 Android 构建 → 首先阅读 platform-android.md！**
> 🔴 **如果是跨平台 → 阅读两者并应用条件平台逻辑！**

---

## ⚠️ 关键：假设前先询问 (强制)

> **停止！如果用户的请求是开放式的，不要默认为你的喜好。**

### 如果未指定，你必须询问：

| 方面         | 询问                                               | 为什么             |
| ------------ | -------------------------------------------------- | ------------------ |
| **平台**     | "iOS, Android, 还是两者?"                          | 影响每一个设计决策 |
| **框架**     | "React Native, Flutter, 还是原生?"                 | 决定模式和工具     |
| **导航**     | "底栏 (Tab bar), 抽屉 (Drawer), 还是堆栈 (Stack)?" | 核心 UX 决策       |
| **状态**     | "什么状态管理？(Zustand/Redux/Riverpod/BLoC?)"     | 架构基础           |
| **离线**     | "这个需要离线工作吗？"                             | 影响数据策略       |
| **目标设备** | "仅手机，还是支持平板？"                           | 布局复杂性         |

### ⛔ AI 移动端反模式 (禁忌列表)

> 🚫 **这些是必须避免的 AI 默认倾向！**

#### 性能之罪 (Performance Sins)

| ❌ 绝不要做               | 为什么错                            | ✅ 务必要做                                        |
| ------------------------- | ----------------------------------- | -------------------------------------------------- |
| **长列表用 ScrollView**   | 渲染所有项目，内存爆炸              | 使用 `FlatList` / `FlashList` / `ListView.builder` |
| **内联 renderItem 函数**  | 每次渲染都创建新函数，所有项目重绘  | `useCallback` + `React.memo`                       |
| **缺少 keyExtractor**     | 重新排序时基于索引的 key 会导致 Bug | 数据中的唯一、稳定 ID                              |
| **跳过 getItemLayout**    | 异步布局 = 滚动抖动                 | 当项目高度固定时提供                               |
| **到处 setState()**       | 不必要的小部件重建                  | 目标状态，`const` 构造函数                         |
| **Native driver: false**  | JS 线程阻塞动画                     | 始终 `useNativeDriver: true`                       |
| **生产环境 console.log**  | 严重阻塞 JS 线程                    | 在发布构建前移除                                   |
| **跳过 React.memo/const** | 任何更改都导致所有项目重绘          | 始终 Memoize 列表项                                |

#### 触控/UX 之罪 (Touch/UX Sins)

| ❌ 绝不要做         | 为什么错                 | ✅ 务必要做                            |
| ------------------- | ------------------------ | -------------------------------------- |
| **触摸目标 < 44px** | 无法准确点击，令人沮丧   | 最小 44pt (iOS) / 48dp (Android)       |
| **目标间距 < 8px**  | 意外点击邻居             | 最小 8-12px 间隙                       |
| **仅手势交互**      | 排除了运动障碍用户       | 始终提供按钮替代方案                   |
| **无加载状态**      | 用户以为 App 崩溃了      | 始终显示加载反馈                       |
| **无错误状态**      | 用户卡住，无恢复路径     | 显示带有重试选项的错误                 |
| **无离线处理**      | 网络丢失时崩溃/阻塞      | 优雅降级，缓存数据                     |
| **忽略平台惯例**    | 用户困惑，肌肉记忆被破坏 | iOS 感觉像 iOS，Android 感觉像 Android |

#### 安全之罪 (Security Sins)

| ❌ 绝不要做               | 为什么错                     | ✅ 务必要做                                               |
| ------------------------- | ---------------------------- | --------------------------------------------------------- |
| **Token 存 AsyncStorage** | 易于访问，在 Root 设备上被盗 | `SecureStore` / `Keychain` / `EncryptedSharedPreferences` |
| **硬编码 API 密钥**       | 从 APK/IPA 逆向工程          | 环境变量，安全存储                                        |
| **跳过 SSL Pinning**      | 这种中间人攻击是可能的       | 在生产环境中固定证书                                      |
| **记录敏感数据**          | 日志可被提取                 | 从不记录 Token，密码，PII                                 |

#### 架构之罪 (Architecture Sins)

| ❌ 绝不要做              | 为什么错             | ✅ 务必要做              |
| ------------------------ | -------------------- | ------------------------ |
| **UI 中写业务逻辑**      | 不可测试，不可维护   | 服务层分离               |
| **万物皆全局状态**       | 不必要的重绘，复杂性 | 默认局部状态，需要时提升 |
| **深度链接作为事后想法** | 通知，分享功能损坏   | 从第一天开始规划深度链接 |
| **跳过 dispose/cleanup** | 内存泄漏，僵尸监听器 | 清理订阅，计时器         |

---

## 📱 平台决策矩阵 (Platform Decision Matrix)

### 何时统一 vs 分歧

```
                    统一 (UNIFY) (两者相同)          分歧 (DIVERGE) (平台特定)
                    ───────────────────           ──────────────────────────
业务逻辑 (Business Logic) ✅ 总是                      -
数据层 (Data Layer)      ✅ 总是                      -
核心功能 (Core Features)  ✅ 总是                      -

导航 (Navigation)        -                             ✅ iOS: 边缘滑动, Android: 返回按钮
手势 (Gestures)          -                             ✅ 平台原生感觉
图标 (Icons)             -                             ✅ SF Symbols vs Material Icons
日期选择器 (Date Pickers) -                             ✅ 原生选择器感觉正确
模态/表单 (Modals/Sheets) -                             ✅ iOS: 底部表单 vs Android: 对话框
排版 (Typography)        -                             ✅ SF Pro vs Roboto (或自定义)
错误对话框 (Error Dialogs) -                             ✅ 警报的平台惯例
```

### 快速参考：平台默认值

| 元素                      | iOS                      | Android               |
| ------------------------- | ------------------------ | --------------------- |
| **主字体**                | SF Pro / SF Compact      | Roboto                |
| **最小触摸目标**          | 44pt × 44pt              | 48dp × 48dp           |
| **返回导航**              | 边缘左滑                 | 系统返回按钮/手势     |
| **底部 Tab 图标**         | SF Symbols               | Material Symbols      |
| **操作表 (Action Sheet)** | 来自底部的 UIActionSheet | Bottom Sheet / Dialog |
| **进度**                  | Spinner                  | 线性进度 (Material)   |
| **下拉刷新**              | 原生 UIRefreshControl    | SwipeRefreshLayout    |

---

## 🧠 移动 UX 心理学 (快速参考)

### 触控的费茨定律 (Fitts' Law)

```
桌面: 鼠标指针精确 (1px)
移动: 手指不精确 (~7mm 接触面积)

→ 触摸目标必须最小 44-48px
→ 重要操作在 拇指区 (屏幕底部)
→ 破坏性操作 远离 容易触及的地方
```

### 拇指区 (单手使用)

```
┌─────────────────────────────┐
│        难以触及              │ ← 导航，菜单，返回
│         (伸展)              │
├─────────────────────────────┤
│        可以触及              │ ← 次要操作
│         (自然)              │
├─────────────────────────────┤
│        容易触及              │ ← 主要 CTA，底栏
│     (拇指的自然弧线)          │ ← 主要内容交互
└─────────────────────────────┘
        [  HOME  ]
```

### 移动端特定的认知负荷

| 桌面             | 移动端差异          |
| ---------------- | ------------------- |
| 多窗口           | 一次一个任务        |
| 键盘快捷键       | 触摸手势            |
| 悬停状态 (Hover) | 无悬停 (点击或没有) |
| 大视口           | 空间有限，垂直滚动  |
| 稳定的注意力     | 不断被打断          |

关于深度解析：[touch-psychology.md](touch-psychology.md)

---

## ⚡ 性能原则 (快速参考)

### React Native 关键规则

```typescript
// ✅ 正确: Memoized renderItem + React.memo 包装器
const ListItem = React.memo(({ item }: { item: Item }) => (
  <View style={styles.item}>
    <Text>{item.title}</Text>
  </View>
));

const renderItem = useCallback(
  ({ item }: { item: Item }) => <ListItem item={item} />,
  []
);

// ✅ 正确: 具有所有优化的 FlatList
<FlatList
  data={items}
  renderItem={renderItem}
  keyExtractor={(item) => item.id}  // 稳定的 ID，不是索引
  getItemLayout={(data, index) => ({
    length: ITEM_HEIGHT,
    offset: ITEM_HEIGHT * index,
    index,
  })}
  removeClippedSubviews={true}
  maxToRenderPerBatch={10}
  windowSize={5}
/>
```

### Flutter 关键规则

```dart
// ✅ 正确: const 构造函数防止重建
class MyWidget extends StatelessWidget {
  const MyWidget({super.key}); // CONST!

  @override
  Widget build(BuildContext context) {
    return const Column( // CONST!
      children: [
        Text('Static content'),
        MyConstantWidget(),
      ],
    );
  }
}

// ✅ 正确: 带有 ValueListenableBuilder 的目标状态
ValueListenableBuilder<int>(
  valueListenable: counter,
  builder: (context, value, child) => Text('$value'),
  child: const ExpensiveWidget(), // 不会重建!
)
```

### 动画性能

```
GPU 加速 (快):            CPU 绑定 (慢):
├── transform               ├── width, height
├── opacity                 ├── top, left, right, bottom
└── (仅使用这些)             ├── margin, padding
                            └── (避免对这些进行动画)
```

关于完整指南：[mobile-performance.md](mobile-performance.md)

---

## 📝 检查点 (FORCE CHECKPOINT) - 任何移动工作前必须完成

> **在编写任何移动代码之前，你必须完成此检查点：**

```
🧠 检查点 (CHECKPOINT):

平台:       [ iOS / Android / Both ]
框架:       [ React Native / Flutter / SwiftUI / Kotlin ]
已读文件:   [ 列出你已读的 skill 文件 ]

我将应用的 3 个原则:
1. _______________
2. _______________
3. _______________

我将避免的反模式:
1. _______________
2. _______________
```

> 🔴 **无法填写检查点？ → 回去阅读 SKILL 文件。**

---

## 🔧 框架决策树

```
你要构建什么？
        │
        ├── 需要 OTA 更新 + 快速迭代 + Web 团队
        │   └── ✅ React Native + Expo
        │
        ├── 需要像素级完美自定义 UI + 性能至关重要
        │   └── ✅ Flutter
        │
        ├── 深度原生功能 + 专注单一平台
        │   ├── 仅 iOS → SwiftUI
        │   └── 仅 Android → Kotlin + Jetpack Compose
        │
        ├── 现有 RN 代码库 + 新功能
        │   └── ✅ React Native (bare workflow)
        │
        └── 企业级 + 现有 Flutter 代码库
            └── ✅ Flutter
```

关于完整决策树：[decision-trees.md](decision-trees.md)

---

## 📋 开发前检查清单 (Pre-Development Checklist)

### 在开始任何移动项目之前

- [ ] **平台已确认？** (iOS / Android / Both)
- [ ] **框架已选择？** (RN / Flutter / Native)
- [ ] **导航模式已决定？** (Tabs / Stack / Drawer)
- [ ] **状态管理已选择？** (Zustand / Redux / Riverpod / BLoC)
- [ ] **离线需求已知？**
- [ ] **深度链接从第一天开始规划？**
- [ ] **目标设备已定义？** (手机 / 平板 / 两者)

### 在每个屏幕之前

- [ ] **触摸目标 ≥ 44-48px？**
- [ ] **主要 CTA 在拇指区？**
- [ ] **存在加载状态？**
- [ ] **存在带有重试的错误状态？**
- [ ] **已考虑离线处理？**
- [ ] **遵循平台惯例？**

### 发布之前

- [ ] **console.log 已移除？**
- [ ] **敏感数据使用 SecureStore？**
- [ ] **SSL pinning 已启用？**
- [ ] **列表已优化 (memo, keyExtractor)？**
- [ ] **卸载时清理内存？**
- [ ] **已在低端设备上测试？**
- [ ] **所有交互元素都有可访问性标签？**

---

## 📚 参考文件 (Reference Files)

关于特定领域的更深层指导：

| 文件                                                   | 何时使用                                       |
| ------------------------------------------------------ | ---------------------------------------------- |
| [mobile-design-thinking.md](mobile-design-thinking.md) | **首先阅读！反死记硬背，强制基于上下文的思考** |
| [touch-psychology.md](touch-psychology.md)             | 理解触摸交互，费茨定律，手势设计               |
| [mobile-performance.md](mobile-performance.md)         | 优化 RN/Flutter，60fps，内存/电池              |
| [platform-ios.md](platform-ios.md)                     | iOS 特定设计，HIG 合规                         |
| [platform-android.md](platform-android.md)             | Android 特定设计，Material Design 3            |
| [mobile-navigation.md](mobile-navigation.md)           | 导航模式，深度链接                             |
| [mobile-typography.md](mobile-typography.md)           | 类型比例，系统字体，a11y                       |
| [mobile-color-system.md](mobile-color-system.md)       | OLED 优化，暗色模式，电池                      |
| [decision-trees.md](decision-trees.md)                 | 框架，状态，存储决策                           |

---

> **记住：** 移动用户是不耐烦的，经常被打断的，并且在小屏幕上使用不精确的手指。为 **最坏** 的情况设计：糟糕的网络，单手，强光，低电量。如果在那里能用，在任何地方都能用。
