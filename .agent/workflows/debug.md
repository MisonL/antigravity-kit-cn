---
description: 调试模式。激活 DEBUG 模式以进行系统化问题排查。
skills:
    - systematic-debugging
    - debugger
---

# 调试命令 (Debug Command)

**触发命令**: `/debug [issue description]`

此工作流是 `/debug` 工作流的快捷入口，直接调用 `debugger` 智能体。

请参考 `agents/debugger.md` 获取详细信息。
