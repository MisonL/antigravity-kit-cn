# 🔄 Agent Flow 架构文档

> **Antigravity Kit CN** - 全流程 AI Agent 工作机制说明
> 对齐上游 Antigravity 流程，并兼容当前分支的 Codex 托管规范（`AGENTS.md` / `antigravity.rules` / `.codex`）。

---

## ✅ 规范对齐基线

- **Antigravity 基线**：Agent/Skill/Workflow 三层结构、Socratic Gate、验证门禁、脚本化检查链路保持与上游一致。
- **OpenAI Codex 基线**：
  - 指令发现遵循 `AGENTS.md` / `AGENTS.override.md` 分层链路。
  - 技能结构遵循 `SKILL.md + scripts/ + references/ + assets/` 规范。
  - 工作区托管文件通过 `AGENTS.md` 与 `antigravity.rules` 注入，不绕过项目级指令链。

---

## 📊 全流程概览图

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户请求 (USER REQUEST)                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                       请求分类 (REQUEST CLASSIFICATION)          │
│  • 识别意图（build/debug/test/deploy 等）                       │
│  • 识别领域（frontend/backend/mobile 等）                       │
│  • 识别复杂度（simple/medium/complex）                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
    ┌───────────────────┐      ┌──────────────────┐
    │  工作流命令入口    │      │  直接智能体分配   │
    │  (Slash Command)  │      │ (Direct Agent)   │
    └─────────┬─────────┘      └────────┬─────────┘
              │                         │
              ▼                         ▼
    ┌───────────────────┐      ┌──────────────────┐
    │ /brainstorm       │      │  按领域选择 Agent │
    │ /create           │      │                  │
    │ /debug            │      │ • frontend-*     │
    │ /deploy           │      │ • backend-*      │
    │ /enhance          │      │ • mobile-*       │
    │ /orchestrate      │      │ • database-*     │
    │ /plan             │      │ • devops-*       │
    │ /preview          │      │ • test-*         │
    │ /status           │      │ • security-*     │
    │ /test             │      │ • game-*         │
    │ /ui-ux-pro-max    │      │                  │
    └─────────┬─────────┘      └────────┬─────────┘
              │                         │
              └────────────┬────────────┘
                           │
                           ▼
         ┌─────────────────────────────────────┐
         │         AGENT 初始化                │
         │  • 加载角色职责                      │
         │  • 加载所需 skills                   │
         │  • 设置行为模式                      │
         └──────────────┬──────────────────────┘
                        │
                        ▼
         ┌─────────────────────────────────────┐
         │        SKILL 加载协议               │
         │                                      │
         │  1. 读取 SKILL.md 元数据            │
         │  2. 按需加载 references/            │
         │  3. 按需调用 scripts/               │
         │  4. 应用规则与模式                  │
         └──────────────┬──────────────────────┘
                        │
                        ▼
         ┌─────────────────────────────────────┐
         │           任务执行层                 │
         │                                      │
         │  • 分析代码库                        │
         │  • 应用最佳实践                      │
         │  • 生成/修改代码                     │
         │  • 运行校验与测试                    │
         └──────────────┬──────────────────────┘
                        │
                        ▼
         ┌─────────────────────────────────────┐
         │          验证与质量门禁              │
         │                                      │
         │  快速检查（checklist.py）            │
         │  • 安全                              │
         │  • 代码质量（lint/types）            │
         │  • Schema                            │
         │  • 测试                              │
         │  • UX                                │
         │  • SEO                               │
         │                                      │
         │  全量检查（verify_all.py）           │
         │  • 以上全部 + Lighthouse             │
         │  • Playwright E2E                    │
         │  • Bundle 分析                       │
         │  • 移动端审计                         │
         │  • i18n 校验                         │
         └──────────────┬──────────────────────┘
                        │
                        ▼
         ┌─────────────────────────────────────┐
         │             结果交付                 │
         │  • 输出变更与结论                    │
         │  • 解释关键决策                      │
         │  • 给出下一步建议                    │
         └─────────────────────────────────────┘
```

---

## 🎯 详细执行流程

### 1️⃣ 请求入口类型

```
用户输入类型：
┌─────────────────────────────────────────────────────────────┐
│ A. 自然语言请求                                              │
│    "构建一个带图表的 React 仪表盘"                          │
│                                                              │
│ B. 斜杠命令                                                  │
│    "/create feature: user authentication"                  │
│                                                              │
│ C. 领域型请求                                                │
│    "优化数据库查询" → database-architect                    │
│    "修复安全漏洞"   → security-auditor                      │
│    "部署到 AWS"    → devops-engineer                        │
└─────────────────────────────────────────────────────────────┘
```

#### 苏格拉底门禁（Socratic Gate）

在任何实现前先做确认：

- **新功能**：至少提出 3 个策略问题
- **Bug 修复**：先复述理解，再确认影响范围
- **模糊需求**：先问目标用户、业务目的、边界范围

### 2️⃣ 智能体选择矩阵

#### Agent 路由检查清单（强制）

在任何代码/设计输出之前必须检查：

| 步骤 | 检查项 | 未满足时操作 |
| ---- | ------ | ------------ |
| 1 | 是否选对 Agent | 先重新分析请求领域 |
| 2 | 是否已读对应 Agent 文档 | 打开 `.agent/agents/{agent}.md` |
| 3 | 是否明确告知用户当前 Agent | 输出 `🤖 正在应用 @[agent] 知识...` |
| 4 | 是否加载 frontmatter 声明的 skills | 检查 `skills:` 并按需读取 |

```
请求领域 → Agent 映射

┌──────────────────────┬─────────────────────┬──────────────────────────┐
│ 领域                  │ 主 Agent            │ 主要 Skills              │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ UI/UX 设计            │ frontend-specialist │ frontend-design          │
│                      │                     │ nextjs-react-expert      │
│                      │                     │ tailwind-patterns        │
│                      │                     │ web-design-guidelines    │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ API 开发              │ backend-specialist  │ api-patterns             │
│                      │                     │ nodejs-best-practices    │
│                      │                     │ python-patterns          │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 数据库设计            │ database-architect  │ database-design          │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 移动端开发            │ mobile-developer    │ mobile-design            │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 游戏开发              │ game-developer      │ game-development         │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 运维/部署             │ devops-engineer     │ deployment-procedures    │
│                      │                     │ server-management        │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 安全审计              │ security-auditor    │ vulnerability-scanner    │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 渗透测试              │ penetration-tester  │ red-team-tactics         │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 测试工程              │ test-engineer       │ testing-patterns         │
│                      │                     │ webapp-testing           │
│                      │                     │ tdd-workflow             │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 问题排查              │ debugger            │ systematic-debugging     │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 性能优化              │ performance-optimizer│ performance-profiling   │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ SEO/GEO              │ seo-specialist      │ seo-fundamentals         │
│                      │                     │ geo-fundamentals         │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 文档编写              │ documentation-writer│ documentation-templates  │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 规划/拆解             │ project-planner     │ brainstorming            │
│                      │                     │ plan-writing             │
│                      │                     │ architecture             │
├──────────────────────┼─────────────────────┼──────────────────────────┤
│ 多 Agent 协同         │ orchestrator        │ parallel-agents          │
│                      │                     │ behavioral-modes         │
└──────────────────────┴─────────────────────┴──────────────────────────┘
```

### 3️⃣ Skill 加载协议

```
┌─────────────────────────────────────────────────────────────┐
│                        SKILL LOADING FLOW                  │
└─────────────────────────────────────────────────────────────┘

Step 1: 请求与 Skill 匹配
┌──────────────────────────────────────────┐
│ 用户："构建 REST API"                    │
│   ↓                                      │
│ 关键词：API → api-patterns               │
└──────────────────────────────────────────┘
                    ↓
Step 2: 加载 Skill 元数据
┌──────────────────────────────────────────┐
│ 读取：.agent/skills/api-patterns/        │
│      └── SKILL.md（主说明）             │
└──────────────────────────────────────────┘
                    ↓
Step 3: 按需加载 references
┌──────────────────────────────────────────┐
│ 读取：api-patterns/rest.md               │
│      api-patterns/graphql.md             │
│      api-patterns/auth.md                │
│      api-patterns/documentation.md       │
└──────────────────────────────────────────┘
                    ↓
Step 4: 按需调用 scripts
┌──────────────────────────────────────────┐
│ 运行：scripts/api_validator.py           │
│      （校验 API 设计）                   │
└──────────────────────────────────────────┘
                    ↓
Step 5: 应用知识
┌──────────────────────────────────────────┐
│ Agent 获得：                             │
│ • API 设计模式                           │
│ • 认证鉴权策略                           │
│ • 文档模板                               │
│ • 自动化校验脚本                         │
└──────────────────────────────────────────┘
```

### 相关 Skill 联动模式

- `frontend-design` 在设计与实现时联动 `web-design-guidelines`
- `clean-code` 在所有 Agent 中作为全局质量约束
- `lint-and-validate` 在交付前作为最终校验层

> 注：脚本不会“无条件自动执行”；应先告知用户并根据流程执行。

### 4️⃣ 工作流命令执行

```
Slash Command Flow

/brainstorm
    ↓
    1. 加载 brainstorming skill
    2. 应用苏格拉底提问
    3. 输出结构化发现文档

/create
    ↓
    1. 识别项目类型（web/mobile/api/game）
    2. 加载 app-builder + 领域技能
    3. 从 app-builder/templates 选模板
    4. 生成项目结构
    5. 运行 checklist.py 校验

/debug
    ↓
    1. 加载 systematic-debugging
    2. 分析错误日志与调用栈
    3. 执行根因分析
    4. 输出修复建议与代码补丁
    5. 验证修复有效

/deploy
    ↓
    1. 加载 deployment-procedures
    2. 识别目标平台（Vercel/AWS/Docker 等）
    3. 准备构建产物
    4. 执行部署步骤
    5. 健康检查
    6. 输出部署地址

/test
    ↓
    1. 加载 testing-patterns + webapp-testing
    2. 识别测试框架（Jest/Vitest/Playwright）
    3. 生成测试用例
    4. 执行测试
    5. 输出覆盖率与结果

/orchestrate
    ↓
    1. 加载 parallel-agents
    2. 任务拆解为子任务
    3. 分配给专业 Agent
    4. 协调执行
    5. 汇总结果
    6. 全量验证

/plan
    ↓
    1. 加载 plan-writing + architecture
    2. 分析需求
    3. 拆解任务与依赖
    4. 输出计划文档（`{task-slug}.md`）

/ui-ux-pro-max
    ↓
    1. 加载 ui-ux-pro-max
    2. 运行设计系统检索
    3. 补充 domain/stack 检索
    4. 基于设计系统实施页面
```

### 5️⃣ 多 Agent 编排

```
复杂任务 → /orchestrate → 多专家协同

示例："构建全栈电商应用"

┌─────────────────────────────────────────────────────────────┐
│                     ORCHESTRATOR AGENT                      │
│  将任务拆解为可执行工作流并控制顺序                           │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│ FRONTEND      │   │ BACKEND       │   │ DATABASE      │
│ SPECIALIST    │   │ SPECIALIST    │   │ ARCHITECT     │
│               │   │               │   │               │
│ 构建：         │   │ 构建：         │   │ 构建：         │
│ • UI/UX       │   │ • REST API    │   │ • Schema      │
│ • Components  │   │ • Auth        │   │ • Migrations  │
│ • Pages       │   │ • Biz Logic   │   │ • Indexes     │
└───────┬───────┘   └───────┬───────┘   └───────┬───────┘
        │                   │                   │
        └─────────────────┬─┴───────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │        一致性控制层                  │
        │  • 接口契约一致                      │
        │  • 类型与导入一致                    │
        │  • 跨模块上下文连续                  │
        └──────────────┬──────────────────────┘
                       │
                       ▼
        ┌─────────────────────────────────────┐
        │          统一验证层                  │
        │  • test-engineer: 测试               │
        │  • security-auditor: 安全            │
        │  • performance-optimizer: 性能       │
        └──────────────┬──────────────────────┘
                       │
                       ▼
        ┌─────────────────────────────────────┐
        │            部署交付                  │
        │  • devops-engineer: 发布             │
        └─────────────────────────────────────┘
```

### 6️⃣ 验证与质量门禁

```
┌─────────────────────────────────────────────────────────────┐
│                    VALIDATION PIPELINE                      │
└─────────────────────────────────────────────────────────────┘

开发阶段（快速检查）：
┌──────────────────────────────────────────┐
│ python .agent/scripts/checklist.py .     │
├──────────────────────────────────────────┤
│ ✓ 安全扫描                               │
│ ✓ 代码质量（ESLint/TypeScript）          │
│ ✓ Schema 校验                            │
│ ✓ 测试套件                               │
│ ✓ UX 审计                                │
│ ✓ SEO 检查                               │
└──────────────────────────────────────────┘
预计耗时：约 30 秒

发布前（全量验证）：
┌──────────────────────────────────────────────────────┐
│ python .agent/scripts/verify_all.py .                │
│        --url http://localhost:3000                   │
├──────────────────────────────────────────────────────┤
│ ✓ 覆盖全部快速检查                                   │
│ ✓ Lighthouse（Core Web Vitals）                      │
│ ✓ Playwright E2E                                     │
│ ✓ Bundle 分析                                        │
│ ✓ 移动端审计                                          │
│ ✓ i18n 校验                                          │
└──────────────────────────────────────────────────────┘
预计耗时：约 3-5 分钟
```

---

## 🧩 Skill 与脚本映射

```
带自动化脚本的 Skill：

┌─────────────────────────┬──────────────────────────────────┐
│ Skill                   │ Script                           │
├─────────────────────────┼──────────────────────────────────┤
│ api-patterns            │ scripts/api_validator.py         │
│ database-design         │ scripts/schema_validator.py      │
│ frontend-design         │ scripts/accessibility_checker.py │
│                         │ scripts/ux_audit.py              │
│ geo-fundamentals        │ scripts/geo_checker.py           │
│ i18n-localization       │ scripts/i18n_checker.py          │
│ lint-and-validate       │ scripts/lint_runner.py           │
│                         │ scripts/type_coverage.py         │
│ mobile-design           │ scripts/mobile_audit.py          │
│ performance-profiling   │ scripts/lighthouse_audit.py      │
│ nextjs-react-expert     │ scripts/react_performance_checker.py │
│ seo-fundamentals        │ scripts/seo_checker.py           │
│ testing-patterns        │ scripts/test_runner.py           │
│ vulnerability-scanner   │ scripts/security_scan.py         │
│ webapp-testing          │ scripts/playwright_runner.py     │
└─────────────────────────┴──────────────────────────────────┘
```

---

## 🔄 完整请求生命周期示例

```
用户请求："构建一个带认证的 Next.js 仪表盘"

1. 请求分类
   ├─ 类型：新功能开发
   ├─ 领域：前端 + 后端
   ├─ 复杂度：中高
   └─ 推荐工作流：/create 或 /orchestrate

2. 选择工作流
   └─ 用户选择：/orchestrate（多 Agent）

3. Orchestrator 拆解
   ├─ Frontend：仪表盘 UI（React 组件）
   ├─ Backend：认证 API（JWT / Session）
   ├─ Database：用户模型（Prisma）
   └─ Testing：认证链路 E2E

4. 分配 Agent
   ├─ frontend-specialist
   │   └─ Skills: nextjs-react-expert, tailwind-patterns, frontend-design
   ├─ backend-specialist
   │   └─ Skills: api-patterns, nodejs-best-practices
   ├─ database-architect
   │   └─ Skills: database-design
   └─ test-engineer
       └─ Skills: testing-patterns, webapp-testing

5. 多域执行（上下文切换）
   说明：这是顺序上下文切换，不是并发线程执行。

   ├─ Frontend 产出：
   │   ├─ app/dashboard/page.tsx
   │   ├─ components/DashboardLayout.tsx
   │   ├─ components/LoginForm.tsx
   │   └─ lib/auth-client.ts
   ├─ Backend 产出：
   │   ├─ app/api/auth/login/route.ts
   │   ├─ app/api/auth/logout/route.ts
   │   ├─ lib/jwt.ts
   │   └─ middleware.ts
   ├─ Database 产出：
   │   ├─ prisma/schema.prisma
   │   └─ prisma/migrations/
   └─ Testing 产出：
       ├─ tests/auth.spec.ts
       └─ tests/dashboard.spec.ts

6. 一致性集成
   └─ AI 在连续生成中保持一致性
      ├─ 导入路径连通
      ├─ 类型契约一致
      └─ UI 与 API 正确对接

7. 验证
   ├─ checklist.py
   │   ✓ 无敏感信息泄露
   │   ✓ 无 ESLint/TS 错误
   │   ✓ 关键测试通过
   └─ verify_all.py
       ✓ E2E: 登录→进入仪表盘→退出
       ✓ 可访问性：符合 WCAG AA
       ✓ 性能：Lighthouse 分数达标

8. 交付
   └─ 用户收到：
      ├─ 完整代码
      ├─ 运行与维护说明
      ├─ 测试结果
      └─ 部署建议
```

---

## 📈 能力统计

```
┌──────────────────────────────────────────────────────────┐
│                    SYSTEM CAPABILITIES                    │
├──────────────────────────────────────────────────────────┤
│ Total Agents:              20                            │
│ Total Skills:              36+                           │
│ Total Workflows:           11                            │
│ Master Scripts:            2 (checklist, verify_all)    │
│ Skill-Level Scripts:       18+                           │
│ Coverage:                  ~90% web/mobile development   │
│                                                          │
│ 典型支持栈：                                              │
│ ├─ Frontend: React, Next.js, Vue, Nuxt, Astro           │
│ ├─ Backend: Node.js, Bun（bun）, FastAPI, Express, NestJS │
│ ├─ Mobile: React Native, Flutter                         │
│ ├─ Database: Prisma, PostgreSQL, SQLite                  │
│ ├─ Testing: Jest, Vitest, Playwright                     │
│ └─ DevOps: Docker（docker）, Vercel, AWS, GitHub Actions │
└──────────────────────────────────────────────────────────┘
```

---

## 🎓 最佳实践

### 何时使用哪个工作流

```
/brainstorm
  ✓ 需求不清晰
  ✓ 需要探索方案
  ✓ 复杂问题需先拆解

/create
  ✓ 现有项目新增功能
  ✓ 中小复杂度
  ✓ 单域任务（前端或后端）

/orchestrate
  ✓ 全栈需求
  ✓ 多阶段复杂任务
  ✓ 需要多专家协同

/debug
  ✓ Bug 报告
  ✓ 异常行为
  ✓ 性能退化

/test
  ✓ 需要补测试覆盖
  ✓ 发布前验证
  ✓ 大改后回归

/deploy
  ✓ 已达到发布条件
  ✓ 全量验证通过
  ✓ 需要生产地址

/plan
  ✓ 大项目拆解
  ✓ 需要工期评估
  ✓ 需要协作排期
```

---

## 🔗 快速索引

- **架构总览**：`.agent/ARCHITECTURE.md`
- **Agents**：`.agent/agents/`
- **Skills**：`.agent/skills/`
- **Workflows**：`.agent/workflows/`
- **脚本入口**：`.agent/scripts/`

---

**Last Updated**: 2026-02-06
**Version**: 2.0.1
